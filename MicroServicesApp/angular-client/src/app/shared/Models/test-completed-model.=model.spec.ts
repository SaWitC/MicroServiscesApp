import { TestCompletedModel } from './test-completed-model.=model';

describe('TestCompletedModel', () => {
  it('should create an instance', () => {
    expect(new TestCompletedModel()).toBeTruthy();
  });
});
